def replace_characters(original_string, old_chars, new_chars):
    """
    Instead of returning null string, this method should return the 
    string by replace specified characters in a string with new characters.
    
    :param original_string: The original string.
    :param old_chars: The characters to be replaced.
    :param new_chars: The new characters to replace the old ones.
    :return: The string with replaced characters.
    """
    return ""

def split_string(original_string, delimiter):
    """
    Instead of returning null string, this method should return
     a list of substrings by Splitting a string based on a delimiter.
    
    :param original_string: The original string to be split.
    :param delimiter: The delimiter used to split the string.
    :return: A list of substrings.
    """
    return ""

def join_strings(string_list, separator):
    """
    Instead of returning null string, this method should Join a list of 
    strings into a single string with a specified separator and return it.
    
    :param string_list: The list of strings to be joined.
    :param separator: The separator used to join the strings.
    :return: The joined string.
    """
    return ""

def count_occurrences(original_string, char):
    """
    Instead of returning 0, this method should return the 
    Count of the occurrences of a character in a string.
    
    :param original_string: The original string.
    :param char: The character to count occurrences of.
    :return: The number of occurrences of the character in the string.
    """
    return 0

def find_char_index(original_string, char):
    """
    Instead of returning 0, this method should return the 
    index of the first occurrence of a character within a string.
    Returns -1 if the character does not exist in the string.
    
    :param original_string: The original string to be searched.
    :param char: A character to search for within the string.
    :return: The index of the first occurrence of the character within the string, or -1 if not found.
    """
    try:
        return 0
    except ValueError:
        return -1

def starts_with_substring(original_string, substring):
    """
    Instead of returning null string, this method should return the boolean value 
    by Checking whether a string starts with a specific substring.
    
    :param original_string: The original string.
    :param substring: The substring to check.
    :return: True if the string starts with the substring, False otherwise.
    """
    return ""

def ends_with_substring(original_string, substring):
    """
    Instead of returning null string, this method should return boolean 
    value by Checking whether a string ends with a specific substring.
    
    :param original_string: The original string.
    :param substring: The substring to check.
    :return: True if the string ends with the substring, False otherwise.
    """
    return ""
